[tool.poetry]
name = "qmk_firmware"
version = "0.1.0"
description = ""
authors = ["emmanuel <emmanuelanaya@outlook.com>"]

[tool.poetry.dependencies]
python = "^3.8"
qmk = "^0.0.34"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
